# 更新日志 / Changelog

## v1.1.0 (2026-08-19)

### 新功能 / New Features
- **Agent 预设汉化**：新会话页与设置中的 Agent 预设显示中文名称与说明（含「Agent预设汉化」开关）
- 预设字典升级为可配置：`settings.yaml` 的 `gui-hanhua.presets` 段（默认 9 条内置，支持覆盖/禁用）
- **AI 自动识别覆盖 Agent 预设**：AI 扫描 `~/.dsh/.agent-presets/` 目录，自动汉化未汉化的预设并写入配置
- **AI 自检覆盖 Agent 预设**：检查 presets 段完整性、与预设目录的匹配性、preset.yml 编码
- **版本检查与自动更新**：启动/手动检查 GitHub 仓库版本（旧/当前/领先三态识别）；发现新版本自动弹窗确认；确认后自动下载并覆盖插件文件（覆盖前自动备份 `.bak`），完成后提示重启
- 设置页「版本更新」区块：状态实时显示 + 检查更新 / 立即更新按钮
- 设置页主标题版本号动态读取（不再硬编码）

### 修复 / Bug Fixes
- **修复 v1.1.0 开发期崩溃**：移除不兼容的 `z.object().partial()`（schemastery 版本不支持，曾导致插件模块加载失败、dsh 启动崩溃）
- schema 构建增加兜底：任何 schema 构建失败自动退回宽松结构，插件始终可加载、GUI 永不因本插件崩溃
- AI 任务提示词中的硬编码用户路径通用化（改为 `<DSH配置目录>` 占位 + 运行时确认），修复隐私泄漏隐患

### 技术 / Internals
- 版本更新通道：host 负责下载/校验/备份/覆盖（Node 可写盘），client 通过 settings 通道传递检查与确认意图
- 更新前备份 `.bak`、校验下载内容（JSON 可解析、文件完整），失败不影响现有版本

## v1.0.0 (2026-08-18)

### 功能 / Features
- 工具卡片汉化（对话中 AI 工具调用卡片中文名称与说明）
- 插件列表汉化（设置 → 插件，230+ 插件中文名）
- 命令面板双语（英文命令名 + 中文说明，支持自定义字典）
- 权限选择器双语（Read Only / Workspace Write / Full access）
- 总开关 + 分项开关 + 配置持久化
- 翻译字典可视化编辑（搜索 / 新增 / 删除 / 启用禁用 / 导入导出 / 恢复默认）
- 智能功能：AI 自动识别（自动汉化未汉化插件）、AI 自检（检查并修复插件问题）
- 常驻加载（web-desktop / web 双 profile 支持）

### 技术 / Internals
- 手写 ModuleLoader bundle（无构建步骤）
- 幂等 patch（dsh-commands 双语钩子、settings 白名单），失败仅告警
- 开源友好：默认字典集中定义、AI 提示词可定制
